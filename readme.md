# 📦 Объекты в JavaScript (JavaScript Objects)

Подробное руководство по объектам в JavaScript: от базовых понятий до тонкостей, которые спрашивают на собеседованиях.

---

## 📑 Содержание
1. [Что такое объект и зачем он нужен](#1-что-такое-объект-и-зачем-он-нужен)
2. [Разница между объектом и массивом](#2-разница-между-объектом-и-массивом)
3. [Структура объекта: Ключи и Значения](#3-структура-объекта-ключи-и-значения)
4. [Базовые операции с объектами](#4-базовые-операции-с-объектами)
5. [Встроенные методы Object.*](#5-встроенные-методы-object)
6. [Вложенные объекты](#6-вложенные-объекты)
7. [Перебор объектов](#7-перебор-объектов)
8. [Сколько методов у объекта](#8-сколько-методов-у-объекта)
9. [Шпаргалка для собеседования (Junior JS Developer)](#9-шпаргалка-для-собеседования-junior-js-developer)

---

## 1. Что такое объект и зачем он нужен?

> **Объект (Object)** — это структура данных, которая позволяет объединить связанные характеристики одного предмета или сущности в одну переменную. 
> 
> *Метафора:* Это своего рода «коробка», на которой написано название предмета, а внутри лежат его свойства.

### Таърифи объект ба забони тоҷикӣ (Для уверенного ответа):
> **Объект** — ин сохтори додаҳо (маълумот) мебошад, ки имкон медиҳад тавсифҳои ба ҳам алоқаманди як ашё ё моҳиятро дар як тағйирёбанда муттаҳид кунем. Ин як намуд «қуттӣ» аст, ки дар болояш номи ашё навишта шудааст ва дар дохили он хосиятҳои он ҷойгиранд.

---

## 2. Разница между объектом и массивом

* **Массив (`Array`)** — это **упорядоченный список**. Элементы идут строго друг за другом и имеют индексы (`0, 1, 2...`). Используется для списков однотипных данных (например, список товаров в корзине).
* **Объект (`Object`)** — это **неупорядоченная структура**. Данные хранятся в формате `ключ: значение`. Порядок не важен, важны имена меток. Используется для детального описания одной конкретной сущности (например, профиль пользователя).

---

## 3. Структура объекта: Ключи и Значения

Объект состоит из свойств (пар `ключ: значение`):
* **Ключ (имя свойства)** — всегда строка (или Symbol), выступает в роли метки.
* **Значение** — может быть абсолютно любым типом данных (строка, число, массив, функция, другой объект).

---

## 4. Базовые операции с объектами

### Создание объекта (Литерал)
```javascript
const ball = {
  brand: "Adidas",
  size: 5,
  isOriginal: true
};
```

## Доступ к свойствам
```js 
// 1. Через точку (основной способ)
console.log(ball.brand); // "Adidas"

// 2. Через квадратные скобки (если ключ динамический или из переменной)
const keyName = "size";
console.log(ball[keyName]); // 5
```

## Изменение и добавление
```js
ball.size = 4;        // Изменение существующего свойства
ball.color = "white"; // Добавление нового свойства
```

## Удаление свойств
```js
delete ball.isOriginal;
```

## 5. Встроенные методы Object.*
Позволяют работать со структурой объекта как с массивом. Данные методы принимают объект в качестве аргумента.
```js 
const laptop = { brand: "Apple", model: "MacBook Air" };
```
- Object.keys(obj) — массив ключей:
```js
Object.keys(laptop); // ["brand", "model"]
```
- Object.values(obj) — массив значений:
```js
Object.values(laptop); // ["Apple", "MacBook Air"]
```
- Object.entries(obj) — массив пар [ключ, значение]:
```js
Object.entries(obj) — массив пар [ключ, значение]:
```
## 6. Вложенные объекты
Значением свойства объекта может быть другой объект, что позволяет создавать глубокие структуры.
```js
const user = {
  id: 42,
  name: "Алексей",
  contacts: {
    email: "alex@example.com",
    phone: "+992900000000"
  }
};

console.log(user.contacts.email); // "alex@example.com"
```
## 7. Перебор объектов
Способ 1: Цикл for...in (Специально для объектов)
```js 
const RGB = { red: "#ff0000", green: "#00ff00" };

for (let key in RGB) {
  console.log(`Ключ: ${key}, Значение: ${RGB[key]}`); // Важно: используем квадратные скобки
}
```

- **Способ 2: Object.entries() + for...of (Современный метод)**
```js 
const shirt = { size: "L", material: "cotton" };

for (const [key, value] of Object.entries(shirt)) {
  console.log(`${key} -> ${value}`);
}
```